# 登录态 cookie（知乎等）绝不进仓库；说明文件除外
secrets/*
!secrets/README.md

# 登录态适配器本体（知乎、小红书）也不进仓库：把某个平台的反爬绕过写成公开
# 代码等于发布一个绕过工具集。lib/adapters.js 在文件存在时自动挂载它。
lib/adapters.private.js

# 本机运维笔记（RSSHub 启动命令、cookie 取法这类只对我这台机器成立的东西）
docs/LOCAL.md

# 抓取缓存：可再生、体积会涨、且是你自己的阅读内容
data/cache.json

# data/subscriptions.json 故意**不**忽略：它就是配置本体，小、可手改、可 diff，
# 而且本项目的发布形态就是「连清单一起发布」（clone 即作者的河）。
# 如果你 fork 出去不想带自己的清单，取消下面这行的注释。
# data/subscriptions.json

# 缓存快照是发布物（clone 后第一次启动的垫底内容），不忽略。
# 全量 cache.json 仍忽略：它可再生，且是你自己的阅读内容。

node_modules/
*.log
.DS_Store

# Codex 桌面端把上一轮的渲染结果落在这里，里面会原样带着对话中出现过的口令、
# AccessKey 这类东西。会话产物不是项目文件，且泄露代价高，一律不进仓库。
.codex-renders/

# make-pages.js 的默认输出目录：快照站是生成物，不进仓库（发布走 gh-pages 分支）
pages-out/

# 关注列表遍历脚本：读登录态 cookie 枚举自己的社交关系，是本机工具，
# 不是产品功能。和 lib/adapters.private.js 同级处理。
tools/zhihu-followees.js

# 遍历结果与筛选过程数据：里面是被关注者的昵称与简介，属于个人社交关系数据。
# 不进主仓也不进公开仓。
data/experiments/
tools/zhihu-probe.js
tools/zhihu-shortlist.js
