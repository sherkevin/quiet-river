'use strict';
const $=id=>document.getElementById(id);
const titles={recommend:'优先阅读',latest:'全部更新',daily:'个性化日报',notes:'我的笔记',sources:'关注来源',health:'采集状态',settings:'阅读偏好'};
let state=null,view='recommend',tag='',source='',offset=0,total=0,runTimer=null,notesCursor='',lastList=[];
const esc=x=>String(x??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const date=x=>x?new Date(x).toLocaleString('zh-CN',{month:'2-digit',day:'2-digit',hour:'2-digit',minute:'2-digit'}):'尚无成功记录';
const label=s=>({NEVER_CHECKED:'尚未检查',NOT_CONFIGURED:'待配置通道',SUCCEEDED_NEW:'已发现新内容',SUCCEEDED_NO_NEW:'检查成功，无新增',AUTH_REQUIRED:'需要重新授权',ACCESS_BLOCKED:'访问受限',COOLDOWN:'等待冷却',TIMEOUT:'采集超时',UPSTREAM_ERROR:'上游异常',QUEUED:'排队中',RUNNING:'更新中',UNSAFE_URL:'不安全地址已阻止',PAUSED:'已暂停'}[s]||s);
async function api(path,body){const r=await fetch('/desk/api'+path,{method:body===undefined?'GET':'POST',headers:body===undefined?{}:{'Content-Type':'application/json','X-QR-Action':'1'},body:body===undefined?undefined:JSON.stringify(body)});const data=await r.json();if(r.status===401){$('login').hidden=false;$('app').hidden=true;}if(!r.ok)throw new Error(data.error||'请求失败');return data;}
function error(e){$('error').textContent=String(e.message||e);$('error').hidden=false;}
function toast(message){$('toast').textContent=message;$('toast').hidden=false;setTimeout(()=>$('toast').hidden=true,3500);}
async function loadState(){state=await api('/state');$('stats').innerHTML=`<span><strong>${state.health.sources}</strong>关注来源</span><span><strong>${state.health.entries}</strong>已保存条目</span><span><strong>${state.health.queue}</strong>等待更新</span>`;const tags=[...new Set(state.sources.flatMap(s=>s.tags||[]))];$('tags').replaceChildren();for(const t of tags){const b=document.createElement('button');b.textContent=t;b.className=tag===t?'active':'';b.onclick=()=>{tag=tag===t?'':t;source='';switchView(['recommend','latest'].includes(view)?view:'recommend');};$('tags').append(b);}}
async function initialize(){try{await loadState();$('login').hidden=true;$('app').hidden=false;await switchView('recommend');refresh();}catch(e){if(!$('login').hidden)return;error(e);}}
$('login-form').onsubmit=async e=>{e.preventDefault();try{await api('/login',{token:new FormData(e.target).get('token')});e.target.reset();$('login-error').textContent='';await initialize();}catch(e){$('login-error').textContent=e.message;}};
document.querySelectorAll('#nav button').forEach(b=>b.onclick=()=>{source='';switchView(b.dataset.view);});
$('refresh').onclick=()=>refresh();
async function refresh(){try{const r=await api('/refresh',{sourceId:source||undefined,tag:tag||undefined});$('task').hidden=false;$('task').textContent=`本轮已提交 ${r.jobs.length} 个通道检查。缓存先展示，检查结果随后更新。`;clearInterval(runTimer);const check=async()=>{try{const x=await api('/runs/'+r.id);const counts={};x.jobs.forEach(j=>counts[label(j.state)]=(counts[label(j.state)]||0)+1);$('task').textContent=Object.entries(counts).map(([k,v])=>`${k} ${v}`).join(' · ')||'该范围暂无已登记的采集通道';if(!x.pending){clearInterval(runTimer);await loadState();if(['latest','recommend'].includes(view))await renderList(false);}}catch(e){clearInterval(runTimer);error(e);}};await check();if(r.pending)runTimer=setInterval(check,4000);}catch(e){error(e);}}
async function switchView(next){view=next;offset=0;notesCursor='';$('title').textContent=titles[view];$('subtitle').textContent=tag?`阅读主题：${tag}`:view==='latest'?'全部已发现内容按时间排列，不因推荐分数低而丢弃。':view==='recommend'?'全部已发现内容保留，本地算法只决定先读什么。':view==='notes'?'高亮和备注来自 Karakeep；没有第二份可编辑笔记库。':'只属于你的阅读工作台。';document.querySelectorAll('#nav button').forEach(b=>b.classList.toggle('selected',b.dataset.view===view));$('more').hidden=true;$('error').hidden=true;try{await loadState();if(['recommend','latest'].includes(view))await renderList(false);else if(view==='sources')renderSources();else if(view==='health')renderHealth();else if(view==='settings')renderSettings();else if(view==='notes')await renderNotes(false);else if(view==='daily')await renderDaily();}catch(e){error(e);}}
function card(e){const a=document.createElement('article');a.className='card';const time=e.published_at?date(e.published_at):'发布时间未知';const stateLabel=e.content_state==='META'?'仅元信息':'已有内容 · 完整性待核验';a.innerHTML=`<div class="meta"><span>${esc(e.source||e.author)}</span><span>${esc(e.platform||'')}</span><span>${esc(time)}</span><span class="badge">${esc(stateLabel)}</span>${e.status==='read'?'<span class="badge">已读</span>':''}</div><h2>${esc(e.title)}</h2><p class="summary">${esc((e.summary||'').slice(0,230))}${e.summary?.length>230?'…':''}</p>${e.reasons?`<div class="reason">${esc(e.reasons.join('；'))}</div>`:''}<div class="actions"><button class="primary" data-action="read">${e.content_state==='META'?'写整篇备注':'站内阅读 / 批注'}</button><a data-original target="_blank" rel="noopener noreferrer">打开原文</a><button data-action="mark">${e.status==='read'?'设为未读':'标记已读'}</button><button data-action="like">${e.feedback===1?'已感兴趣':'感兴趣'}</button><button data-action="dislike">${e.feedback===-1?'已降权':'不感兴趣'}</button></div>`;
const original=a.querySelector('[data-original]');try{const u=new URL(e.url);if(!['http:','https:'].includes(u.protocol))throw new Error();original.href=u.href;}catch{original.remove();}
a.querySelector('[data-action=read]').onclick=async event=>{const b=event.currentTarget;b.disabled=true;b.textContent='准备阅读内容…';try{const result=await api(`/entries/${e.id}/archive`,{});window.open(result.path,'_blank','noopener,noreferrer');toast(result.state==='QUEUED'?'正文已提交阅读器，解析可能尚在进行':'已打开阅读器');}catch(e){error(e);}finally{b.disabled=false;b.textContent='站内阅读 / 批注';}};
a.querySelector('[data-action=mark]').onclick=async()=>{try{const target=e.status==='read'?'unread':'read';await api(`/entries/${e.id}/read`,{status:target});e.status=target;a.querySelector('[data-action=mark]').textContent=target==='read'?'设为未读':'标记已读';}catch(e){error(e);}};
for(const [name,value] of [['like',1],['dislike',-1]])a.querySelector(`[data-action=${name}]`).onclick=async()=>{try{await api(`/entries/${e.id}/feedback`,{value:e.feedback===value?0:value});toast('反馈已保存，将用于本地排序');}catch(e){error(e);}};
return a;}
async function renderList(append){if(!append){offset=0;$('content').replaceChildren();}const q=new URLSearchParams({mode:view,offset:String(offset),limit:'30'});if(tag)q.set('tag',tag);if(source)q.set('source',source);const page=await api('/entries?'+q);total=page.total;lastList=page.items;if(!total){$('content').innerHTML='<div class="empty"><h2>这段河流还在汇入。</h2><p>检查更新后，新发现的帖子会出现在这里。<br>有通道失败时，请查看“采集状态”，不能把失败当作没有更新。</p></div>';}else for(const e of page.items)$('content').append(card(e));offset+=page.items.length;$('more').hidden=offset>=total;$('more').onclick=()=>renderList(true).catch(error);}
function renderSources(){const container=$('content');container.innerHTML='<div class="toolbar"><button id="add-source">添加来源</button><a href="/desk/api/sources/export">导出完整来源台账</a></div><div id="source-list"></div>';$('add-source').onclick=()=>{container.innerHTML=`<form id="source-form" class="card"><h2>添加你选择的博主</h2><p class="muted">有原生或逐博主转发 Feed 时直接填写；只有主页时先登记，不伪装成已接通。</p><label>名称<input name="name" required maxlength="150"></label><label>主页<input name="url" type="url"></label><label>RSS / Atom 地址<input name="feedUrl" type="url"></label><label>平台<select name="platform"><option value="blog">博客</option><option value="wechat">公众号</option><option value="twitter">X</option><option value="zhihu">知乎</option><option value="xiaohongshu">小红书</option><option value="bilibili">B站</option><option value="github">GitHub</option></select></label><label>主题（逗号分隔）<input name="tags"></label><button>保存来源</button></form>`;$('source-form').onsubmit=async event=>{event.preventDefault();const data=Object.fromEntries(new FormData(event.target));data.tags=data.tags.split(/[,，]/).map(x=>x.trim()).filter(Boolean);try{await api('/sources',data);await switchView('sources');}catch(e){error(e);}};};for(const s of state.sources){const row=document.createElement('div');row.className='source';const cs=state.health.channels.filter(c=>c.sourceId===s.id);row.innerHTML=`<div><strong>${esc(s.name)}</strong><p>${esc(s.platform)} · ${esc((s.tags||[]).join(' / '))}</p><p>${cs.length?cs.map(c=>esc(label(c.state))).join(' · '):'尚无采集通道，仍保留登记'}</p></div><div class="actions"><button data-open>查看</button><button data-refresh>刷新</button><button data-pause>${s.enabled?'暂停采集':'恢复采集'}</button></div>`;row.querySelector('[data-open]').onclick=()=>{source=s.id;tag='';switchView('latest');};row.querySelector('[data-refresh]').onclick=()=>{source=s.id;tag='';refresh();};row.querySelector('[data-pause]').onclick=async()=>{try{await api('/sources/'+s.id,{enabled:!s.enabled});await switchView('sources');}catch(e){error(e);}};$('source-list').append(row);}}
function renderHealth(){const byId=new Map(state.sources.map(s=>[s.id,s]));$('content').innerHTML='<div class="card"><h2>成功检查，不等于作者发了文章</h2><p class="summary">这里记录最后成功检查时间。凭证组失败会暂停整组，避免对每位作者重复敲同一份失效凭证。未配置通道不会被标为正常。</p></div>';for(const g of state.health.groups.filter(g=>g.state!=='OK'&&g.state!=='UNKNOWN')){const a=document.createElement('div');a.className='card';a.innerHTML=`<h2>${esc(g.id)}</h2><p>${esc(label(g.state))}</p><button>凭证已更新，允许下一次验证</button>`;a.querySelector('button').onclick=async()=>{try{await api('/groups/resume',{id:g.id});await switchView('health');}catch(e){error(e);}};$('content').append(a);}for(const c of state.health.channels){const a=document.createElement('div');a.className='source';a.innerHTML=`<div><strong>${esc(byId.get(c.sourceId)?.name||c.sourceId)}</strong><p>${esc(c.transport)} · 最后成功：${esc(date(c.lastSuccess))}</p><p>${esc(c.error)}</p></div><span class="badge">${esc(label(c.state))}</span>`;$('content').append(a);}}
function renderSettings(){const pref=state.preferences||{},tags=[...new Set(state.sources.flatMap(s=>s.tags||[]))];$('content').innerHTML=`<form id="preferences" class="card"><h2>本地推荐偏好</h2><p class="muted">-3 降低优先级，0 中性，3 优先。标签下来源数量不会自动成为兴趣权重。</p><div class="weights">${tags.map(t=>`<label>${esc(t)}<input type="number" min="-3" max="3" step="1" data-tag="${esc(t)}" value="${Number(pref.tags?.[t]||0)}"></label>`).join('')}</div><label>近期关注关键词（逗号分隔）<textarea name="keywords" rows="3">${esc(pref.keywords||'')}</textarea></label><label>日报篇数<input name="digestCount" type="number" min="1" max="50" value="${pref.digestCount||15}"></label><label>日报时区<input name="timezone" value="${esc(pref.timezone||'Asia/Shanghai')}"></label><button>保存偏好</button><p class="muted">第一版不调用云端模型。平台评分、阅读反馈和日报结果均保存在本机。</p></form>`;$('preferences').onsubmit=async e=>{e.preventDefault();const data=Object.fromEntries(new FormData(e.target));data.tags={};e.target.querySelectorAll('[data-tag]').forEach(x=>data.tags[x.dataset.tag]=Number(x.value));data.authors=pref.authors||{};try{await api('/preferences',data);toast('偏好已保存');}catch(e){error(e);}};}
async function renderNotes(append){if(!append){notesCursor='';$('content').innerHTML='<div class="toolbar"><a href="/desk/api/notes/export">导出高亮为 Markdown</a><a href="/dashboard">进入原生阅读器查看全部备注</a></div>';}
const page=await api('/notes'+(notesCursor?'?cursor='+encodeURIComponent(notesCursor):''));if(page.notConfigured){$('content').innerHTML='<div class="empty"><h2>阅读器账户尚未完成连接</h2><p>正文存储与高亮由 Karakeep 提供，当前不能把这项能力标为可用。</p></div>';return;}const list=page.highlights||[];if(!list.length&&!append)$('content').insertAdjacentHTML('beforeend','<div class="empty">还没有高亮。打开一篇站内文章后，选中文字即可记录。</div>');for(const h of list){const a=document.createElement('article');a.className='card';a.innerHTML=`<div class="meta">${esc(date(h.createdAt))}</div><blockquote>${esc(h.text)}</blockquote><p class="note">${esc(h.note||'')}</p><a href="/dashboard/preview/${encodeURIComponent(h.bookmarkId)}">回到原文批注</a>`;$('content').append(a);}notesCursor=page.nextCursor||'';$('more').hidden=!notesCursor;$('more').onclick=()=>renderNotes(true).catch(error);}
async function renderDaily(){const list=await api('/digests');$('content').innerHTML='<div class="toolbar"><button id="make-digest">生成 / 更新今日日报</button><select id="digest-select"></select></div><div id="digest-body"></div>';for(const d of list){const o=document.createElement('option');o.value=d.day;o.textContent=d.day;$('digest-select').append(o);}const show=async day=>{const d=await api('/digests/'+day);$('digest-body').innerHTML=`<div class="card"><h2>${esc(d.day)} · 你的优先阅读</h2><p class="summary">已从本地候选内容中选取 ${d.items.length} 篇。未入选的文章仍在全量列表中。</p><p class="muted">${d.issues.length} 个通道尚未成功检查；本日报不代表完整的原平台更新。</p></div>`;for(const e of d.items)$('digest-body').append(card(e));};$('digest-select').onchange=()=>show($('digest-select').value).catch(error);$('make-digest').onclick=async()=>{try{const day=new Intl.DateTimeFormat('en-CA',{timeZone:state.preferences.timezone||'Asia/Shanghai',year:'numeric',month:'2-digit',day:'2-digit'}).format(new Date());await api('/digest',{day,regenerate:true});await renderDaily();}catch(e){error(e);}};if(list.length)await show(list[0].day);else $('digest-body').innerHTML='<div class="empty">日报尚未生成。内容入库后可以立即生成，不依赖外部模型。</div>';}
initialize();
