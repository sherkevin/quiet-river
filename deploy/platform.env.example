# Compose interpolation file; pin official images to verified digest before production.
MINIFLUX_IMAGE=ghcr.io/miniflux/miniflux:2.3.3
KARAKEEP_IMAGE=ghcr.io/karakeep-app/karakeep:0.33.2
# Actual credentials belong ONLY in /etc/quiet-river-platform/*.env (0600).
