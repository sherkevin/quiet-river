# 私人阅读平台：恢复开发记录（2026-09-17）

## 状态声明

这是开发中检查点，不是完整交付。不得将代码存在、单元测试通过、容器健康和真实来源验收混为一谈。

## 已完成的恢复

- 重新发现 ECS 设备，新会话运行身份为 root。旧设备 ID 不再使用。
- 检查时没有遗留开发终端；在指定时间范围内没有发现内核 OOM 记录。
- 发现中断前留下的 Bridge、UI、测试和部署脚本，并先做私有备份。
- 旧生产目录、运行数据、环境配置和 systemd unit 已建立 root-only 恢复点。
- 新文件提交为 `10da4aa`；随后合并主分支 `2ea0958`，形成 `450caf7`。
- 合并保留上游宿主熔断与退避改动，并保留严格的逐端口 401 部署自检。
- 开发分支 `chatgpt/reader-platform-v1` 已真实推送，不改 main。

## 已运行的验证

- 合并主分支之前，Node 22 串行执行 119 项测试全部通过。
- 上述结果不代表合并后的完整回归，也不代表 Node 20 兼容性通过。
- 官方 Miniflux 2.3.3 和 Karakeep 0.33.2 镜像已在 ECS 独立回环端口启动。
- Miniflux 初次启动失败：数据库 URI 实际仍拨号容器内 localhost。
- 改为显式 UNIX socket 关键字 DSN 后，Miniflux 健康检查返回 OK。
- 真实 API：OPML 建立禁用调度的测试 Feed，返回 201。
- 真实 API：主动导入未读测试文章，返回 201；标记已读返回 204。
- 真实 API：PUT 更新正文返回 201，随后查询仍为已读。
- 测试 Feed 已删除。测试不访问真实博主，也没有更改私网保护开关。

## 安全检查与中断界限

本轮部分远程执行请求被 OpenAI 工具安全检查拒绝；不能将其描述为 ECS 权限不足或进程崩溃。
设备连接仍可用于读取与 Git 推送，远程工具额度检查显示尚余约 98%。
无法从这些信息确定上一轮空响应的内部原因；没有证据支持“需要继续加权限”。
被拒绝的线上口令复用和批量代码修改没有执行，不计入完成项。

## 仍未完成，不得按生产可用发布

- Reader Bridge 尚未建立 systemd 运行实例或公开入口。
- Karakeep 账号、API 连接、高亮写入和恢复尚未通过真实端到端验收。
- 271 个来源的逐源迁移、真实刷新、缺失通道和凭证恢复尚未执行。
- ntfy、日报投递、跨应用笔记入口和恢复演练仍未完成。
- 所有浏览器采集与跨服务并发预算仍需验证，不得凭 root 权限声明可用。
- 当前测试使用 `node:sqlite`；Node 20 CI 必须隔离这些测试或提供明确运行版本判断。
- 暂停采集与隐藏历史、未知发布时间、部分正文状态需要补充回归测试。
- 当前页面只在整轮刷新完成后重载条目，应改为显示处理中已经取得的更新。
- 当前通用来源配置会关闭原生正文提取，需改为保留并逐源验证其配置。
- 高亮列表不等于全部笔记；现有导出尚未完整包含整篇备注及来源字段。
- 119 项测试不能替代这些待验收项，也不能替代 72 小时稳定运行观察。

## 运行状态与下一检查点

本轮没有替换或重启旧 quiet-river 生产应用，也没有覆盖线上订阅清单。
新增核心服务仅绑定本机回环地址；没有开放新的公网数据库或阅读器端口。
测试时总内存 1740 MiB，无 Swap；启动阅读核心后可用内存约 291 MiB。
这只是一次采样，不是完整负载容量保证。不要在该基线上直接并发启动浏览器采集。
下一检查点是合并后代码回归、可恢复的部署定义和受保护的阅读器账号连接，之后才能切换日常入口。
